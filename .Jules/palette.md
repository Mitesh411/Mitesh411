# Palette's Journal

## 2024-05-22 - Accessibility in Markdown Profiles
**Learning:** Developers often rely on `title` attributes on links for context but neglect `alt` attributes on the images inside, potentially leaving screen reader users without description if the link text is empty.
**Action:** Always verify `alt` text on images inside links, ensuring it conveys the same meaning as the visual badge or icon.
